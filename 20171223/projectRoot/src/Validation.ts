// 导出接口

export interface StringValidator {
  isAcceptable(s: string): boolean;
}

// 导入 import { StringValidator } from './Validation';

// 导出接口
// export default interface StringValidator {
//   isAcceptable(s: string): boolean;
// }
// // 导入接口
// import validator from './Validation';